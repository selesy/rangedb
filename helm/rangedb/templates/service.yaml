apiVersion: v1
kind: Service
metadata:
  name: {{ include "rangedb.fullname" . }}
  labels:
    {{- include "rangedb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rangedb.selectorLabels" . | nindent 4 }}
