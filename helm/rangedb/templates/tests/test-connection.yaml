apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rangedb.fullname" . }}-test-connection"
  labels:
    {{- include "rangedb.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rangedb.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
